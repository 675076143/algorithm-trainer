import React from "react";
import { StyleSheet } from "react-native";
import { Text } from "../Themed";

export default function BubbleSort() {
  return <Text>BubbleSort</Text>;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
